
// Just the part of the API before the '.execute' 
// And after the https://
var API_ID = "REPLACE_ME"

// Your bucket name
var S3_BUCKET = "REPLACE_ME" + "-photos-dev"

// Leave These
var API_URL_BASE = "https://" + API_ID + ".execute-api.us-east-1.amazonaws.com/dev/"
var S3_URL = "https://" + S3_BUCKET + ".s3.amazonaws.com/"
